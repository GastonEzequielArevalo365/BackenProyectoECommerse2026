//Aquí guardamos en una variable todas las rutas de productos de dicha carpeta.
const productosRoute = require('./productos.route')

//Recordar que estamos enviando un objeto con clave : valor, pero como se llama igual el atributo que el valor solo lo ponemos una vez.
module.exports = {productosRoute}

